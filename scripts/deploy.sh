#!/usr/bin/env bash
# Deploys infra (cdk deploy --all) then builds + deploys frontend to Amplify.
# Fill in once infra stacks are ready.
set -e
echo "TODO: cdk deploy --all"
echo "TODO: amplify publish (or git push if using Amplify's git-based deploy)"
