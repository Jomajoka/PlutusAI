"""
Defines: Cognito user pool + app client + API Gateway authorizer.
Part of the foundation (plan Section 2a): every API route uses the authorizer,
and business_id is derived from the verified token (Users table / custom claim).
"""
