// Chat UI for both insight questions and what-if scenarios.
// Calls api/client.js#sendChatMessage, renders ChatMessage list + any
// returned chart via ChartPanel.
export default function ChatPanel({ sessionId }) { return null; } // TODO
